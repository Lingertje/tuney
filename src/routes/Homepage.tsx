import { Layout } from "components/Layout";
import { FC } from "react";

const Homepage: FC = () => {
  return (
    <Layout>
      <h1>This is the homepage</h1>
    </Layout>
  );
};

export { Homepage };
